import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  tabContainer: {
    alignContent: 'center',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    height: 50,
  },
  tabImagen: {
    height: 30,
    width: 30,
  }
})