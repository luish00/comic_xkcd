import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  tab: {
    flexDirection: 'row',
  },
  tabContainer: {
    alignContent: 'center',
    alignItems: 'center',
    display: 'flex',
    height: 50,
    justifyContent: 'center',
  },
  tabImagen: {
    height: 30,
    width: 30,
  },
});
